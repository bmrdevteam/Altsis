# Alt Board — 기획 문서

## 개요

Alt Board는 Altsis 내에서 문서, 양식, 시트를 통합 관리하는 멀티 기능 보드 시스템이다.
구글 클래스룸 + Docs + Forms + Sheets의 학교 특화 버전으로,
수업 계획서와 연동하여 수업 활동의 중심 허브 역할을 한다.

---

## 핵심 구조

```
Alt Board
├── [Docs 탭]   — 게시글 목록 (머지 문법 사용 가능)
├── [Form 탭]   — 양식 목록 (각 Form에 Sheet가 종속)
├── [Sheet 탭]  — 시트 목록 (Form별 Sheet 바로 접근)
├── [채팅 탭]   — 보드 단위 그룹 대화
└── (스트림 뷰)  — Docs + Form 통합 타임라인 (Phase 5)
```

### 핵심 원칙

- **Form이 스키마의 단일 진실 공급원**: Sheet의 컬럼은 Form 필드에서만 생성됨
- **Sheet는 Form 종속**: 독립 생성 불가, Form 생성 시 자동 생성됨
- **Docs는 기존 게시글 활용**: 새 에디터가 아니라 기존 Post에 머지 기능만 추가

### 모듈 연결 관계

```
Form 생성  ──→  Sheet 자동 생성 (필드 = 컬럼)
Form 응답  ──→  Sheet에 row 자동 추가
Sheet 데이터  ──→  Docs(게시글)에서 {{변수}} 머지
```

---

## 각 모듈 상세

### Alt Docs (문서)

- **기존 게시글(Post) 기능을 그대로 활용**
- 모든 게시글에서 머지 문법 사용 가능 (일반 게시글과 머지 Docs의 구분 없음)
- `{{#sheet}}`가 없는 게시글은 일반 게시글로 동작
- .md 파일 다운로드
- 첨부파일 지원 (기존 기능)

#### 머지 기능 (동적 데이터 바인딩)

하나의 템플릿 문서가 열람자에 따라 동적으로 렌더링된다.
정적 문서 N개를 생성하는 것이 아니라, Sheet 데이터를 실시간으로 읽어 표시한다.

**권한**: Sheet 열람 권한이 있는 사용자만 머지 Docs를 생성할 수 있다.

**데이터 소스 연결**: 문서 상단에 `{{#sheet 시트명}}`으로 데이터 소스를 명시한다.
하나의 머지 Docs에는 **하나의 Sheet만 연결** 가능하다.

```
{{#sheet 중간고사}}

{{학생이름}}님의 중간고사 성적
국어: {{국어}}점 / 수학: {{수학}}점
총점: {{총점}}점
```

**열람 동작**:

```
열람자              동작
─────────          ──────
응답자 (학생)       Sheet에서 _respondent = 나인 행 자동 매칭 → 변수 치환
Sheet 열람 권한자   상단에 사용자 검색/선택 UI → 선택한 사용자의 행으로 변수 치환
```

**템플릿 문법**:

| 문법 | 동작 |
|------|------|
| `{{#sheet 시트명}}` | 데이터 소스 Sheet 선언 (문서 상단, 1개만) |
| `{{변수}}` | 첫 번째 행의 값 (행이 1개일 때 사용) |
| `{{#each}} ... {{/each}}` | 매칭된 모든 행을 반복 |
| `{{#table 컬럼1, 컬럼2, ...}}` | 매칭된 행을 테이블로 렌더링 |
| `{{_count}}` | 매칭된 행 수 |

**예시 — 성적 통지서 (행 1개)**:

```
{{학생이름}}님의 중간고사 성적

국어: {{국어}}점 / 수학: {{수학}}점 / 영어: {{영어}}점
총점: {{총점}}점
```

**예시 — 과제 현황 (행 여러 개)**:

```
{{#each}}
- {{과제제목}}: {{점수}}점 ({{제출일}})
{{/each}}

총 {{_count}}건 제출

{{#table 과제제목, 점수, 제출일}}
```

**추가 기능**:
- 다운로드: 현재 렌더링된 결과를 .md로 저장
- 복제: 기존 게시글을 복제하여 새 게시글 생성

### Form/Docs 복제 및 내보내기

```
복제:
  보드 내 기존 Docs/Form → [복제] → 새 아이템 생성 (데이터 제외, 구조만 복제)
  Form 복제 시 → 빈 Sheet도 함께 생성

내보내기/가져오기:
  Docs → .md 다운로드 → 다른 보드에서 .md 업로드하여 게시글 생성
  Form → .json 내보내기 → 다른 보드에서 .json 가져오기하여 Form + Sheet 생성
```

- 복제 시 Sheet 데이터는 포함하지 않음 (구조만 복제)
- 다른 학기/다른 보드에서 같은 양식을 재사용할 때 유용

### Alt Form (양식)

- 폼 빌더 (기존 Survey 시스템을 기반으로 확장)
- **Form 생성 시 Sheet 자동 생성** (필드 → 컬럼 매핑)
- 응답 → 연결된 Sheet에 자동 저장
- .json 내보내기/가져오기
- 예약 기능은 중복 검사 기능으로 통합 (별도 예약 시스템 및 전용 필드 타입 불필요)

#### Form 라이프사이클

```
Form 설정:
  공개 시작: [2026-03-10 09:00]   ← 이 시간부터 학생에게 표시
  공개 종료: [2026-03-17 23:59]   ← 이 시간 이후 제출 불가

  재제출:    ☑ 허용  ← 제출 취소 후 재제출 가능
```

- 공개 전: 학생에게 Form이 보이지 않음 (교사만 편집 가능)
- 공개 중: 학생이 Form 응답 가능
- 공개 종료: 제출 불가 (마감 후 제출 허용하지 않음)
- 재제출: 제출 취소 → Sheet 행 삭제 → 새로 제출 (응답 직접 수정은 불가)

#### Form 설정 호환성

모든 설정은 독립적이며 자유롭게 조합 가능하다.

| 조합 | 사용 예시 |
|------|----------|
| 퀴즈 모드 + 중복 검사 | 같은 시험을 두 번 볼 수 없게 하면서 자동 채점 |
| 직접 입력 모드 + 제출 확인 | 출석부 — 교사가 입력 완료한 학생과 미입력 학생 추적 |
| 승인 + 중복 검사 | 면담 예약 — 슬롯 선택 + 교사 승인 |
| 퀴즈 모드 + 마감일 | 시간 제한 퀴즈 — 마감 후 정답/점수 공개 |

#### 제출 확인

보드 멤버 목록과 Sheet 응답 데이터를 대조하여 제출 현황을 표시한다.

```
교사 뷰 — 제출 현황:
  총 30명 중 22명 제출 (73%)

  ● 제출    김학생, 박학생, 이학생 ...  (22명)
  ○ 미제출  최학생, 정학생 ...          (8명)

  [미제출자에게 알림 보내기]

학생 뷰 — Form 상단:
  ✓ 제출 완료 (3/15 14:23)    [수정] [제출 취소]
  ─ 또는 ─
  ○ 미제출                    [제출하기]
```

- 제출 여부: Sheet에 `_respondent = 나`인 행 존재 여부로 판단
- 재제출 허용 여부: Form 설정으로 제어
- 마감일 이후 제출 허용 여부: Form 설정으로 제어

#### 퀴즈 모드

Form에 퀴즈 모드를 켜면 필드별로 정답과 배점을 설정할 수 있다.

```
Form 설정:
  ☑ 퀴즈 모드

필드별 설정 (퀴즈 모드일 때 표시):
  질문: "대한민국의 수도는?"
  선택지: ○ 서울  ○ 부산  ○ 대전
  정답: 서울 ✓
  배점: [10]점
```

**자동 채점**:
- 가능: select, radio, checkbox, number (정확 일치)
- 불가: text, textarea, file → 교사가 Sheet에서 수동 채점

**Sheet 자동 컬럼** (퀴즈 모드일 때 추가):

```
| 질문1  | 질문2  | ... | _score | _total |
| 서울 ✓ | 부산 ✗ | ... | 80     | 100    |
```

**결과 공개 설정**:

```
점수 공개:  ○ 제출 즉시  ○ 마감 후  ○ 비공개
정답 공개:  ○ 제출 즉시  ○ 마감 후  ○ 비공개
오답 표시:  ☑ 틀린 문항 표시
```

#### 필드 타입

```
기본 필드:
  - text (단답)
  - textarea (장문)
  - number (숫자)
  - date (날짜)
  - select / multiSelect (선택)
  - checkbox
  - radio
  - file (파일 업로드)
  - counter (카운터) — 응답 수 제한용 특수 필드

특수 필드:
  - userSelect (학교 사용자 선택)
  - approval (승인) — 승인자 선택 + 승인/반려 워크플로우
  - rating (평점)
  - scale (척도)
```

#### 필드 유효성 검사

각 필드에 유효성 검사 규칙을 설정할 수 있다:

```
공통:
  - 필수 입력 여부

타입별:
  - text / textarea: 최소/최대 글자 수, 정규식 패턴
  - number: 최소/최대 값
  - date: 날짜 범위 (이후/이전)
  - file: 허용 확장자, 최대 용량
  - select / multiSelect: 최소/최대 선택 수
```

#### 조건부 필드 표시

앞 필드의 응답에 따라 뒷 필드를 보여주거나 숨길 수 있다:

```
필드 설정:
  표시 조건: [지각 사유] = [기타]  ← 이 조건을 만족할 때만 필드 표시

예시:
  ① 지각 사유: ○ 교통  ○ 건강  ○ 기타
  ② 상세 사유: [____________]  ← "기타" 선택 시에만 표시

조건 연산자:
  - = (같음), ≠ (다름)
  - 포함 (multiSelect/checkbox용)
  - 비어있음, 비어있지 않음
```

- 숨겨진 필드의 값은 Sheet에 빈 값으로 저장
- 여러 조건을 AND/OR로 결합 가능

#### 필드 타입: counter (카운터)

응답 수를 제한하는 특수 필드. 정해진 수량에 도달하면 추가 제출을 차단한다.

```
필드 설정:
  최대 수량: [30]건

동작:
  제출 시 → 현재 응답 수 카운트 → 30건 미만이면 제출 허용
  30건 도달 → 이후 제출 차단 ("마감" 표시)

Form에 표시:
  모집 현황: 22 / 30명
```

- 중복 검사와 조합 가능: 카운터는 전체 응답 수, 중복 검사는 특정 조합별 수량 제한
- 예: 선착순 30명 모집, 수강 신청 정원 관리

#### 필드 타입: approval (승인)

승인자를 선택하고 승인/반려 워크플로우를 수행하는 필드.

```
응답자가 보는 Form:
  승인자: [김교사 ▼]  ← 사용자 선택

Sheet에 저장:
  | 승인자 | 승인 상태 | 승인 시간 | 사유 |
  | 김교사 | pending  |          |     |

승인자가 보는 Sheet:
  [승인] [반려]  사유: [____________]
```

- 승인 상태: pending → approved / rejected
- 승인/반려 시 응답자에게 알림 발송

**승인자 접근 경로**: 별도 승인 대시보드 없이 알림 시스템을 활용한다.

```
즉시:  알림 수신 → 클릭 → Sheet 해당 행으로 이동 → 승인/반려
확인:  보드 → Sheet → 필터 (승인자=나, 상태=pending)
통합:  알림 목록 → 타입 필터 "승인 대기" → 전체 보드의 미처리 건 확인
```

**다중 승인**: approval 필드를 여러 개 배치하여 다단계 승인 가능.

```
Form 설정:
  승인 방식: ○ 순차  ○ 동시

순차: 1차 approved → 2차 알림 발송 → 2차 approved → 완료
      1차 rejected → 이후 단계 건너뜀
동시: 모든 승인자에게 동시 알림 → 전원 approved → 완료
      한 명이라도 rejected → 전체 반려
```

#### 필드 옵션: 중복 검사

각 필드에 "중복 검사" 체크를 할 수 있다. 중복 검사가 켜진 필드들의 값 조합이
이미 Sheet에 존재하면 제출을 차단한다.

```
필드 옵션:
  ☑ 중복 검사

  모드:
    ○ 자유      — 필드 자체 옵션에서 자유 선택, 중복만 검사
    ● 사전 등록  — Sheet에 등록된 조합만 선택 가능 (정확한 값 매칭)

  허용 수량: [1]건  ← 같은 조합 최대 몇 건까지 허용
```

| 모드 | 선택지 출처 | 용도 |
|------|-----------|------|
| 자유 | 필드 자체 옵션 | 중복 제출 방지, 수강 정원 관리 |
| 사전 등록 | Sheet 행 | 예약, 좌석 배정 등 유효 조합 제한 |

#### 캐스케이딩 필터

중복 검사 필드가 여러 개일 때, 앞 필드 선택값을 기반으로
뒷 필드의 가용 옵션을 자동 필터링한다.

```
예시 - 면담 예약:
  ① 날짜 선택: [3/10 ▼]
     → Sheet에서 3/10에 해당하는 행 조회

  ② 교시 선택:
     1교시 (선택 가능)  ← (3/10, 1교시) 잔여 있음
     2교시 (마감)       ← (3/10, 2교시) 허용 수량 초과
     3교시 (선택 가능)
```

#### 사전 등록 데이터 입력 방법

사전 등록 모드에서 유효한 조합을 Sheet에 등록하는 3가지 경로:

```
소량: Sheet에서 직접 행 입력
대량: 조합 생성기 (Form 설정 내 일회성 도구)
외부: CSV 가져오기
```

**조합 생성기**: 중복 검사(사전 등록) 필드가 2개 이상일 때 Form 설정에 표시되는
일회성 유틸리티. 필드 타입에 따라 자동으로 적절한 입력 UI를 제공한다.

```
┌─ 조합 생성 ──────────────────────────────────┐
│  ① 날짜 (date):                              │
│     시작: [2026-03-02]  끝: [2026-04-27]      │
│     반복: ☑매주  요일: ☑월                     │
│     → 8건                                    │
│                                              │
│  ② 교시 (select):                            │
│     ☑ 1교시  ☑ 2교시                          │
│     → 2건                                    │
│                                              │
│  ③ 강의실 (select):                           │
│     ☑ 101호                                  │
│     → 1건                                    │
│                                              │
│  생성 결과: 8 × 2 × 1 = 16건                  │
│  [Sheet에 생성]                               │
└──────────────────────────────────────────────┘
```

- 설정은 저장되지 않음 (일회성 실행)
- 생성 후 미세 조정은 Sheet에서 직접 편집
- 필드 타입별 UI: date → 범위+반복, select → 체크박스, text → 목록, number → 범위

#### 필드 권한 설정

각 필드에 입력 권한을 지정할 수 있다:

```
필드 권한:
  - respondent (응답자 입력)  — 학생이 Form에서 입력
  - owner (작성자 전용)       — 교사가 Sheet에서만 입력
    └ 응답자에게 공개: ☐ (기본 비공개, 켜면 학생이 Sheet에서 읽기 전용으로 열람)

예시 - 과제 Form:
  - 과제 제목     (respondent)             → 학생이 Form에서 입력
  - 파일 업로드    (respondent)             → 학생이 Form에서 입력
  - 점수          (owner, 응답자에게 공개 ✓)  → 교사가 입력, 학생이 열람 가능
  - 피드백        (owner, 응답자에게 공개 ✓)  → 교사가 입력, 학생이 열람 가능
  - 내부 메모     (owner)                  → 교사만 열람

학생이 보는 Form:   [과제 제목] [파일 업로드]
학생이 보는 Sheet:  [과제 제목] [파일 업로드] [점수 🔒] [피드백 🔒]
교사가 보는 Sheet:  [과제 제목] [파일 업로드] [점수] [피드백] [내부 메모]
```

#### 직접 입력 모드

응답을 수집하지 않고 교사가 직접 Sheet에 기록하는 용도로도 사용 가능:

```
예시 - 출석부 Form:
  - 학생       (owner, userSelect) → _respondent로 매핑
  - 날짜      (owner)
  - 출석 여부  (owner)
  → Form이 "스키마 정의서" 역할, 실제 데이터 입력은 Sheet에서
```

**`_respondent` 매핑**: 직접 입력 모드에서는 userSelect 필드를 `_respondent`로 매핑하거나,
사용자 ID를 직접 입력하여 해당 행의 소유자를 지정한다.
이를 통해 머지 Docs에서 학생별 데이터 매칭이 가능해진다.

#### 예약을 Form으로 대체하는 흐름

```
교사: Form 생성 (날짜 + 교시 필드: 중복 검사 사전 등록 + approval 필드: 승인자)
  → 교사: Sheet에 유효 슬롯 등록 (직접 입력 / 조합 생성기 / CSV)
  → 학생: Form 응답 (캐스케이딩 필터로 가용 슬롯만 표시 + 승인자 선택)
  → Sheet: 해당 행에 respondent + 승인자 채워짐
  → 승인자: 알림 수신 → Sheet에서 승인/반려
  → 학생: 결과 알림 수신
```

- 동시성 제어: 제출 시 백엔드에서 atomic하게 해당 조합의 현재 count 확인
- 승인이 필요하면 approval 필드 추가, 불필요하면 생략 (별도 시스템 컬럼 없음)

### Alt Sheet (시트)

- **Form 종속**: Form 생성 시 자동 생성, 독립 생성 불가
- **컬럼 = Form 필드**: 컬럼 추가/삭제는 Form 필드 수정으로만 가능
- **셀 값 편집 가능**: 교사가 점수, 상태 등을 직접 수정
- **행 입력**: 직접 입력, 다중 행 복사/붙여넣기
- .csv 다운로드 / .csv 가져오기
- 뷰: 컬럼별 검색 필터, 정렬, 컬럼 숨기기

#### 컬럼별 검색 필터

각 컬럼 헤더에 검색어를 입력하면 해당 조건에 맞는 행만 표시한다.
여러 컬럼에 동시에 검색어를 넣으면 AND 조건으로 동작한다.

```
| 날짜 [3/10  ] | 교시 [     ] | 응답자 [김  ] | 승인 상태 [pending ▼] |
|----------------|-------------|--------------|---------------------|
| 3/10           | 1교시       | 김학생        | pending             |
| 3/10           | 2교시       | 김영희        | pending             |
  ↑ 날짜="3/10" AND 응답자에 "김" 포함된 행만 표시
```

- text / textarea: 부분 문자열 검색
- select / radio: 드롭다운 선택
- date: 날짜 범위 (시작 ~ 끝)
- number: 범위 (이상 ~ 이하)
- checkbox: 체크됨 / 안됨
- approval: 상태별 필터 (pending / approved / rejected)

#### 자동 시스템 컬럼

Form 필드 외에 자동으로 추가되는 시스템 컬럼:

```
- _respondent (응답자)
- _submittedAt (응답 시간)
- _updatedAt (수정 시간)
```

#### 학생의 본인 응답 열람

학생은 Sheet를 통해 본인 응답을 열람한다.

```
학생이 Sheet에 접근하면:
  → _respondent = 나인 행만 필터되어 표시
  → 교사 전용(owner) 컬럼은 기본 비공개, "응답자에게 공개" 옵션이 켜진 컬럼만 표시
```

- Form을 다시 열면 이전 응답이 아닌 새 제출 화면이 표시됨
- 응답 확인은 Sheet에서 처리
- 응답 수정은 불가 — 제출 취소 후 재제출만 가능

#### 행 삭제 및 응답 철회

Sheet 데이터가 단일 진실 공급원이다.

```
학생이 제출 취소  →  Sheet에서 해당 행 삭제  →  다시 제출 가능
교사가 행 삭제    →  학생의 제출 상태 = 미제출  →  다시 제출 가능
```

- 중복 검사 사전 등록 행: 교사가 등록한 빈 행은 삭제하면 해당 슬롯 자체가 사라짐
- 응답이 채워진 사전 등록 행: 학생이 철회하면 행이 빈 상태(응답자 없음)로 복원됨

#### Form 필드 변경 시 Sheet 동기화

- 필드 추가 → Sheet 컬럼 자동 추가, 기존 행에는 빈 값
- 필드 삭제 → Sheet 컬럼 삭제 + 해당 데이터 삭제
- 필드 옵션 변경 (선택지 수정, 타입 변경 등) → 기존 데이터는 변경하지 않음
- Form이 유일한 스키마 소스이므로 동기화 충돌 불가

---

## 보드 운영 방식

### 두 가지 모드

| 모드 | 설명 | 멤버 |
|------|------|------|
| 독립 보드 | 자유롭게 생성 (동아리, 부서, 프로젝트 등) | 초대 기반 |
| 수업 보드 | 수업 계획서에 1:1 연결 | 수강생 자동 동기화 |

### 권한 모델

보드의 권한은 역할 기반이 아니라 **설정 기반**이다.
교사/학생이라는 학교 역할과 무관하게, 보드 내에서 권한을 자유롭게 부여할 수 있다.

```
보드 권한:
  - 관리자    — 보드 설정, 멤버 관리, 모든 기능 접근
  - 작성자    — Docs/Form 생성, Sheet 전체 열람/편집
  - 응답자    — Form 응답, 본인 행만 Sheet에서 열람

수업 보드 기본값:
  교사 → 관리자
  학생 → 응답자

독립 보드:
  생성자 → 관리자
  초대 시 역할 지정 (학생도 관리자/작성자 가능)
```

- 예: 학생 동아리 보드에서 학생이 관리자, 교사가 응답자가 될 수 있음
- 예: 학생 프로젝트에서 학생이 Form을 만들고 다른 학생이 응답

### 수업 계획서 연동

수업 계획서 페이지 내에 Alt Board가 탭으로 들어간다:

```
수업 계획서 페이지
├── [수업 정보]  기존 탭
├── [수업 계획]  기존 탭
├── [수강생]    기존 탭
└── [Alt Board]  새 탭
    ├── 스트림 뷰 (Docs + Form 통합 타임라인)
    ├── 마감 임박 표시
    └── [+ 새로 만들기] → 문서 / 양식
```

- 수업 계획서 생성 시 Alt Board 자동 생성
- 멤버 = 수강생 (Enrollment 기반 자동 동기화)
- 교사 = 보드 관리자/작성자
- 학생 = 보드 멤버/응답자
- 학기/과목 맥락 자동 부여

### 수업 보드 활용 예시

| 활동 | 구현 방식 |
|------|----------|
| 수업 자료 배포 | Alt Docs (기존 게시글) |
| 공지사항 | Alt Docs (기존 게시글) |
| 과제 제출 | Alt Form (기한 + 파일 업로드 + 교사 전용 채점 필드) |
| 퀴즈/설문 | Alt Form |
| 예약 (면담 등) | Alt Form (중복 검사 사전 등록 모드) |
| 출석부 | Alt Form (직접 입력 모드) → Sheet |
| 성적 관리 | Alt Form (교사 전용 필드) → Sheet |
| 통지서 발급 | Sheet 데이터 → Alt Docs 템플릿 머지 |

### 학생 관점 흐름

```
내 수업 목록
├── 국어 → 수업 계획서 → Alt Board 탭 → 과제 확인/제출
├── 수학 → 수업 계획서 → Alt Board 탭 → 퀴즈 응답
└── 영어 → 수업 계획서 → Alt Board 탭 → 자료 열람
```

---

## 채팅 시스템 재편

### 기본 방향

```
채팅 시스템
├── DM (1:1)          — 기존 시스템 유지
└── 보드 채팅 (그룹)   — Alt Board 내부
    ├── 수업 보드 채팅  — 수강생 자동 참여
    └── 독립 보드 채팅  — 보드 멤버 참여
```

- 1:1 대화: 기존 채팅 시스템에서 처리
- 그룹 대화: 보드 내부에서 처리 (별도 그룹 채팅방 제거)
- 보드 멤버 = 채팅 참여자 (자동, 별도 초대 불필요)

### 기존 그룹 채팅의 문제

| 기존 그룹 채팅 | 보드 내부 채팅 |
|--------------|--------------|
| 채팅방 생성 + 멤버 초대 | 보드 멤버 = 채팅 참여자 (자동) |
| 대화 맥락 없음 | 수업/프로젝트 맥락 내장 |
| 파일 공유하면 채팅에 묻힘 | Docs/Form/Sheet가 바로 옆에 |
| 수업별로 방을 따로 만들어야 함 | 수업 보드에 자동 포함 |

### 보드 내 채팅 구조

```
Alt Board
├── 보드 채팅 (메인)  — 보드 전체 공지/토론, 보드 멤버 전원 참여
│
├── Alt Docs  → 댓글 스레드 (문서별 토론)
└── Alt Form  → 댓글 스레드 (양식별 토론)
```

- **보드 채팅**: 보드 단위의 메인 대화 공간
- **아이템 댓글**: 특정 문서/양식에 대한 스레드형 토론

### 기존 시스템 변경

- 그룹 채팅 모델/기능 제거
- 1:1 DM 시스템만 기존 채팅에 유지
- 보드 채팅 모델 신규 생성 (Board에 종속)

---

## 캘린더 연동

### 기본 방향

보드별 캘린더를 따로 두지 않고, **전체 캘린더에 통합**한다.
캘린더의 목적은 한 곳에서 모든 일정을 보는 것이므로, 분산시키지 않는다.

### 동기화 방식

기존 캘린더의 `sourceType` 패턴을 확장한다:

```
sourceType 확장:
  - enrollment   (기존)
  - syllabus     (기존)
  - memo         (기존)
  - altForm      (양식 마감일, 예약 일정)
  - altDocs      (문서 공개일)
  - altBoard     (보드 직접 등록 일정)
```

### 자동 반영 대상

```
Alt Board 아이템                    캘린더 이벤트
─────────────────                  ──────────────
Form (마감일 있음)        ──→      "국어 3주차 과제 마감" (3/15)
Form 예약 확정 (중복 검사) ──→      "국어 교사 면담" (3/20 14:00)
Docs (공개일 지정)        ──→      "중간고사 범위 공지" (3/10)
```

### 양방향 연결

- **보드 → 캘린더**: Form 마감일 설정 → 캘린더에 자동 표시
- **캘린더 → 보드**: 캘린더에서 이벤트 클릭 → 해당 Form/Docs로 바로 이동
- 캘린더 필터로 보드별/수업별 일정 분리 가능

### 보드 내부에서는 캘린더 대신 일정 요약

스트림 뷰 상단에 마감 임박 항목만 표시한다.

---

## 알림 정책

알림은 최소화한다. 사용자가 놓치면 안 되는 것만 알림으로 보낸다.

```
알림 대상 (필수):
  - 승인 요청 수신 (승인자에게)
  - 승인/반려 결과 (응답자에게)

알림 대상 아님 (알림 없음):
  - Form 제출 완료 (본인은 이미 알고 있음)
  - 새 문서/양식 게시 (스트림 뷰에서 확인)
  - 채팅 메시지 (채팅 내부 배지로 처리)
  - 마감 임박 (스트림 뷰 상단에서 확인)
  - 퀴즈 결과 (학생이 Sheet에서 직접 확인)
```

- 미제출 알림은 교사가 수동으로 발송 (자동 아님)
- 이후 필요에 따라 알림 항목을 선택적으로 추가 가능

---

## 구글 워크스페이스 대비 차별점

| 영역 | Google Workspace | Altsis Alt Board |
|------|-----------------|------------------|
| 학생 매핑 | 이메일 초대 필요 | 수강 신청 데이터로 자동 |
| 권한 | 수동 공유 설정 | 학교 역할 기반 자동 |
| 맥락 | 범용 도구 | 학기/과목/학급 맥락 내장 |
| 환경 | 앱 전환 필요 | 캘린더, 채팅, 보드 한 곳에 |
| 계정 | Google 계정 필수 | 자체 계정 체계 |

---

## 리팩터링 계획

> 현재 Board/Post/Survey/Reservation 코드는 개발 완료 상태이나 운영 중이 아님.
> 사용자 데이터 마이그레이션 없이 자유롭게 구조 변경 가능.

### 제거 (코드 삭제)

| 기존 코드 | 대체 | 활용 가능한 부분 |
|-----------|------|----------------|
| Reservation 모델/컨트롤러 | Form 중복 검사 + Sheet | 승인 워크플로우 로직 |
| ReservationSlot 모델/컨트롤러 | Form 사전 등록 + Sheet | 슬롯 관리 UI 참고 |
| SurveyResponse 모델 | Sheet row | - |
| 그룹 채팅 기능 | 보드 채팅 | 채팅 UI/소켓 로직 재사용 |

### 리팩터링 (구조 변경)

| 기존 코드 | 변경 방향 | 재사용 범위 |
|-----------|----------|------------|
| Board 모델 | Alt Board (독립/수업 모드 추가) | 멤버/권한 구조 대부분 유지 |
| Post 모델 | Alt Docs로 전환 | 에디터, 첨부파일 로직 그대로 유지 |
| Survey 빌더/렌더러 | Alt Form으로 확장 | 질문 타입, 빌더 UI 상당 부분 재사용 |
| 채팅 시스템 | DM 전용으로 축소 | 1:1 채팅 로직 그대로 유지 |

### 신규 개발

| 모듈 | 내용 |
|------|------|
| Alt Sheet | 모델 + 컨트롤러 + 테이블 뷰/편집 UI (Form 종속) |
| Form → Sheet 연동 | Form 생성 시 Sheet 자동 생성 + 응답 자동 저장 |
| Form 필드 권한 | respondent / owner 권한 설정 |
| Form 중복 검사 | 자유/사전 등록 모드, 캐스케이딩 필터, 조합 생성기 |
| Sheet → Docs 머지 | `{{변수}}` 템플릿 바인딩 엔진 |
| 보드 채팅 | 보드 종속 그룹 채팅 모델/UI |
| 캘린더 sourceType 확장 | altForm, altDocs 동기화 |
| 수업 계획서 탭 | Alt Board 탭 UI + 수강생 자동 동기화 |

---

## 개발 단계 (안)

> 전략: 모듈별 횡적 개발이 아닌, 하나의 시나리오를 끝까지 관통하는 종적 개발.
> 목표 시나리오: "과제 출제 → 학생 제출 → 교사 채점 → 통지서 생성"

```
Phase 1: 종적 관통 (MVP)
  목표: "과제 → 채점 → 통지서" 시나리오가 실제로 작동

  - Alt Board 기본 (수업 보드 모드 + 수강생 자동 매핑)
  - Alt Form 최소 기능 (text, file, date 필드 + 교사 전용 필드)
  - 필드 유효성 검사 (필수, 글자 수, 숫자 범위 등)
  - Form → Sheet 자동 생성 + 응답 자동 저장
  - Alt Sheet 최소 기능 (테이블 뷰, 셀 편집)
  - Alt Docs 머지 기능 (기존 게시글 + {{변수}} 바인딩)
  - 수업 계획서 내 Alt Board 탭 연결
  - 캘린더 마감일 동기화

Phase 2: Form 확장 + 중복 검사
  - 나머지 필드 타입 (select, radio, scale, rating, approval, counter 등)
  - 중복 검사 (자유 모드 + 사전 등록 모드)
  - 캐스케이딩 필터
  - 조합 생성기
  - 조건부 필드 표시
  - 직접 입력 모드 (교사용 출석부 등)
  - 퀴즈 모드 (자동 채점 + 결과 공개 설정)
  - 제출 확인 (현황 표시 + 미제출자 수동 알림)
  - 승인 알림 (최소 알림: 승인 요청/결과만)
  - JSON 내보내기/가져오기
  - Form/Docs 복제
  - 기존 Reservation/ReservationSlot 코드 제거

Phase 3: Sheet + Docs 확장
  - Sheet: 필터, 정렬, 컬럼 숨기기, CSV 다운로드/가져오기, 복사/붙여넣기
  - Docs: .md 다운로드

Phase 4: 채팅 재편
  - 그룹 채팅 → 보드 채팅으로 전환 (기존 채팅 UI/소켓 재사용)
  - 기존 채팅을 DM 전용으로 축소
  - 아이템별 댓글 스레드

Phase 5: 고도화
  - 스트림 뷰 (통합 타임라인 + 마감 임박)
  - 독립 보드 모드 + 유연한 권한 모델
  - 캘린더 ↔ 보드 양방향 연결
```
